services:
  api:
    container_name: api
    # image: python:latest
    build:
      context: ./api
      dockerfile: ./api/Dockerfile.orange
    # volumes:
    #   - ./api:/usr/src/app
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    ports:
      - 8443:8443
    environment:
      APP: api
    # command: /bin/sh

  db:
    container_name: db
    image: postgres:latest
    volumes:
      - ./database/postgres_data:/var/lib/postgresql/data
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    ports:
      - 5432:5432
    environment:
      APP: db
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: sivrg_db
